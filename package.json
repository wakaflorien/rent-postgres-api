{
"name": "rental-platform",
"version": "1.0.0",
"private": true,
"description": "A property rental platform with host and renter capabilities",
"workspaces": [
    "backend",
    "frontend"
],
"scripts": {
    "start": "npm run --workspace backend start",
    "dev": "npm run --workspace backend dev",
    "build": "npm run --workspace backend build",
    "test": "npm run --workspace backend test",
    "lint": "npm run --workspace backend lint"
},
"author": "",
"license": "ISC",
"engines": {
    "node": ">=16.0.0",
    "npm": ">=10.0.0"
},
"devDependencies": {
    "typescript": "^4.9.5"
}
}

